## Summary

Brief description of the changes in this PR.

## Changes

- Change 1
- Change 2

## Testing

- [ ] `make clean && make` builds without warnings
- [ ] `bash tests/run_tests.sh` passes
- [ ] `make format` applied (no unformatted code)
- [ ] New strategies include `can_handle` + `apply` functions
- [ ] Verification scripts pass on affected test fixtures
- [ ] If fixtures changed: updated `tests/fixtures/manifest.yaml` and added rationale for fixture add/remove/rename

## Related Issues

Closes #
